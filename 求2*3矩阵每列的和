#include<stdio.h>
int main()
{
	int a[2][3], i, j, sum;
	for (i = 0; i < 6; i++)
	{
		scanf_s("%d", (a[0] + i), 1);
	}
	for (j = 0; j < 3; j++)
	{
		sum = 0;
		for (i = 0; i < 2; i++)
		{
			sum += a[i][j];
		}
		printf("%d ", sum);
	}
}
