#include<stdio.h>
int main()
{
	int i;
	float n, a, b;
	scanf_s("%f", &n,1);
	a = n;
	b = n / 2.0;
	for (i = 0; i < 10; i++)
	{
		a += 2*b;
		b = b / 2.0;
	}
    b=2*b;
	printf("第10次落地时共经过%f米\n第10次反弹%f米\n",a,b);
}
