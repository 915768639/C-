#include<stdio.h>
int main()
{
	int n, i = 1, s = 1, m, c = 1, sum = 0;
	scanf("%d", &n);
	while (i <= n)
	{
		
		while (s <= i)
		{
			c = c * s;
			s++;
		}
		sum = sum + c;
		i++;
	}
	printf("%d", sum);
}
